apiVersion: v1
kind: Service
metadata:
  name: nginx-plus-live-dashboard
  namespace: nginx-ingress
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 38080
    protocol: TCP
    name: http
  selector:
    app: nginx-ingress